<%= form_for [@context, @reminder] do |f| %>
  <div class="form-group">
    <%= f.label :subject %>
    <%= f.text_area :subject, class: "form-control", placeholder: "Enter a reminder here.", rows: 10 %>
  </div>

  <%= f.submit class: "btn btn-primary" %>
<% end %>