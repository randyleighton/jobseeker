<% content_for(:title, "Companies") %>

<div class="row show-header-box">
  <div class="col-xs-12 col-sm-6 col-md-4 show-header-title">
    <h2>Companies <%= link_to new_company_path, class: "btn btn-adds", title: "Add a new Company" do %>
    <%= content_tag(:i,nil,class: 'fa fa-plus fa-2x')%>
    Add New
  <% end %></h2>
  </div>

  <div class="col-xs-12 col-sm-6 col-md-8">
    <p>Companies are your launch point for just about everything. Create a company to start and from there everything falls in place. There are 4 key parts to your companies: contacts, the job you applied for, followup actions, and the company information. <%= link_to "Create or select a company", companies_path, title: "View the company list to create new one" %> and start your path to a new job!</p>
  </div>

</div>

<div class="row companies-ul">
	<ul class="fa-ul">
	  <% @companies.all.each do |company| %>
	    <li>
        <%= link_to company_path(company), title: "View #{company.name}" do %>
          <%= company.name %>
          <% if company.reminders.count > 0 %>
            <%= content_tag(:i,nil,class: 'fa-reminder fa fa-bell')%> 
          <% end %>
        <% end %>
      </li>
	  <% end %>
	</ul>
</div>
